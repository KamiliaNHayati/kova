[project]
name = 'kova'
description = ''
authors = []
telemetry = false
cache_dir = '.\.cache'
requirements = []
[contracts.agent-wallet-v4]
path = 'contracts/agent-wallet-v4.clar'
clarity_version = 3
epoch = 'latest'

[contracts.service-registry-v3]
path = 'contracts/service-registry-v3.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
