[project]
name = 'kova'
description = ''
authors = []
telemetry = false
cache_dir = '.\.cache'
requirements = []
[contracts.agent-wallet-v2]
path = 'contracts/agent-wallet-v2.clar'
clarity_version = 3
epoch = 'latest'

[contracts.service-registry-v2]
path = 'contracts/service-registry-v2.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
